<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wangm.dao.OrderMapper">

    <insert id="insert">
       insert into
       t_order
       (
       order_name,
       order_status,
       order_token,
       order_id
       )
       values
       (
       #{orderName},
       #{orderStatus},
       #{orderToken},
       #{orderId}
       )

   </insert>
</mapper>
